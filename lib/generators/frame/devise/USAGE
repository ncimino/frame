Description:
    Generates devise installation.

Usage:
    Use to generate install and setup devise for a project.

Examples:
    rails generate frame:devise

Methods:
    There are several methods generated which you can use in your application.
