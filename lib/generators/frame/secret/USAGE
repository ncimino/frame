Description:
    Generates config files with private information.

Usage:
    Use to generate secret files for a project, such as database.yml.

Examples:
    rails generate frame:secret

Methods:
    There are several methods generated which you can use in your application.
